/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'standalone',
  basePath: '', // Remove the custom basePath
  assetPrefix: '', // Remove the assetPrefix
  trailingSlash: true,
  images: {
    unoptimized: true,
  },
}

module.exports = nextConfig;
