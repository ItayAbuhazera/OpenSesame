/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/OpenSesame',
  assetPrefix: '/OpenSesame/',
  trailingSlash: true,
  images: {
    unoptimized: true,
  },
  // Add this to handle images and other static files
  publicRuntimeConfig: {
    basePath: '/OpenSesame',
  },
}

module.exports = nextConfig